#Codigo para problema 3
  #el apartado 1 es fácil (con un sum(x))

#Para extraer numeros
x <- 10:20
x
x[-c(3,5,7)] #esto extrae los elementos en las posiciones 3,5 y 7

#apartado 2
sum(exp(x))-sum(exp(y)) #con sus respectivos valores
